const router=require("express").Router();const {authenticate}=require("../middlewares/auth.middleware");const {place,verify,getMyOrders,getMyOrder,webhook}=require("../controllers/order.controller");router.post("/webhook", webhook);router.use(authenticate);router.get("/",getMyOrders);router.get("/:orderId",getMyOrder);router.post("/",place);router.post("/verify",verify);module.exports=router;
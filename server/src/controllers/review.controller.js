const Review=require("../models/review.model");const Product=require("../models/product.model");const ApiError=require("../utils/ApiError");const ApiResponse=require("../utils/ApiResponse");const asyncHandler=require("../utils/asyncHandler");
const list=asyncHandler(async(req,res)=>{const product=await Product.findOne({$or:[{SKU:req.params.productId},{slug:req.params.productId}]});if(!product)throw new ApiError(404,"Product not found");const reviews=await Review.find({product:product._id,status:"approved"}).populate("user","name").sort({createdAt:-1});res.json(new ApiResponse(200,reviews,"Reviews fetched"))});
const create=asyncHandler(async(req,res)=>{const product=await Product.findOne({$or:[{SKU:req.params.productId},{slug:req.params.productId}]});if(!product)throw new ApiError(404,"Product not found");const {rating,text}=req.body;if(!Number.isInteger(rating)||rating<1||rating>5||!text?.trim())throw new ApiError(400,"Rating and review text are required");const existing=await Review.findOne({product:product._id,user:req.user._id});if(existing)throw new ApiError(409,"You have already reviewed this product");const review=await Review.create({product:product._id,user:req.user._id,rating,text});res.status(201).json(new ApiResponse(201,review,"Review submitted for moderation"))});const adminList=asyncHandler(async(req,res)=>{const reviews=await Review.find().populate("product","SKU data.Title").populate("user","name email").sort({createdAt:-1});res.json(new ApiResponse(200,reviews,"Reviews fetched"))});const moderate=asyncHandler(async(req,res)=>{const review=await Review.findById(req.params.reviewId);if(!review)throw new ApiError(404,"Review not found");if(!["approved","rejected"].includes(req.body.status))throw new ApiError(400,"Invalid review status");review.status=req.body.status;await review.save();res.json(new ApiResponse(200,review,"Review moderated"))});const remove=asyncHandler(async(req,res)=>{const review=await Review.findByIdAndDelete(req.params.reviewId);if(!review)throw new ApiError(404,"Review not found");res.json(new ApiResponse(200,null,"Review deleted"))});module.exports={list,create,adminList,moderate,remove};
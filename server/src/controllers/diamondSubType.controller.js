const DiamondSubType = require("../models/diamondSubType.model"); const ApiError = require("../utils/ApiError"); const ApiResponse = require("../utils/ApiResponse"); const asyncHandler = require("../utils/asyncHandler"); const list = asyncHandler(async (req,res) => res.json(new ApiResponse(200, await DiamondSubType.find().sort({name:1}).lean(), "Diamond subcategories fetched"))); const create = asyncHandler(async (req,res) => { const name=String(req.body.name||"").trim(); if(!name) throw new ApiError(400,"Diamond subcategory name is required"); const row=await DiamondSubType.findOneAndUpdate({name},{ $setOnInsert:{name}},{new:true,upsert:true,setDefaultsOnInsert:true}); res.status(201).json(new ApiResponse(201,row,"Diamond subcategory saved")); }); module.exports={list,create};
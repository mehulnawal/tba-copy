const GST_RATE = 0.03;
const requiredNumber = (value, label) => { if (value === undefined || value === null || value === "" || !Number.isFinite(Number(value))) throw new Error(`${label} is required for pricing`); if (Number(value) < 0) throw new Error(`${label} cannot be negative`); return Number(value); };
const calculateSilverPrice = ({ product, rates, config }) => { if (!config || config.metal !== "silver") throw new Error("Silver product requires a silver category pricing configuration"); const grossWeight = requiredNumber(product?.grossWeight, "Gross weight"); const silverRate = requiredNumber(rates?.silver, "Fine silver rate"); const makingRate = requiredNumber(config.makingRatePerGram, "Silver making rate per gram"); const metalValue = grossWeight * silverRate; const makingValue = grossWeight * makingRate; const stoneWeight = product?.diamondCaratWeight ?? product?.moissaniteCaratWeight; const stoneValue = stoneWeight === undefined || stoneWeight === null ? 0 : requiredNumber(stoneWeight, "Moissanite carat weight") * requiredNumber(config.stoneRatePerUnit, "Moissanite rate per ct"); const totalCost = metalValue + makingValue + stoneValue; const gst = totalCost * GST_RATE; return { metal: "silver", silverRate, grossWeight, metalValue, makingRatePerGram: makingRate, makingValue, stoneValue, totalCost, gst, finalPrice: totalCost + gst }; };
module.exports = { calculateSilverPrice };
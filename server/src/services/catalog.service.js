const Product = require("../models/product.model"); const { calculatePrice } = require("../utils/priceCalculator");
const slugify = value => String(value || "").toLowerCase().trim().replace(/[^a-z0-9]+/g,"-").replace(/(^-|-$)/g,"");
const toProductResponse = doc => { const product = doc.toObject ? doc.toObject() : doc; return { ...product, id: String(product._id), Title: product.title, Description: product.description, Category: product.subCategory?.name || "", "image_link-1": product.images?.[0]?.url || "", "image_link-2": product.images?.[1]?.url || "", "image_link-3": product.images?.[2]?.url || "", Is_Best_Seller: product.isBestSeller, Is_New_Product: product.isNewProduct, prices: ["9kt","14kt","18kt"].map(k => calculatePrice(product,k)) }; };
module.exports = { toProductResponse, slugify };

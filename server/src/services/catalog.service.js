const { calculatePrice } = require("../utils/priceCalculator");
const slugify = value => String(value || "").toLowerCase().trim().replace(/[^a-z0-9]+/g, "-").replace(/(^-|-$)/g, "");
const toProductResponse = async doc => { const product = doc.toObject ? doc.toObject() : doc; const buyer = product.buyerType || "B2C"; const karats = product.metal === "silver" ? [undefined] : ["14kt", "18kt", "24kt"]; return { ...product, id: String(product._id), Title: product.title, Description: product.description, Category: product.subCategory?.name || "", "image_link-1": product.images?.[0]?.url || "", "image_link-2": product.images?.[1]?.url || "", "image_link-3": product.images?.[2]?.url || "", Is_Best_Seller: product.isBestSeller, Is_New_Product: product.isNewProduct, prices: await Promise.all(karats.map(karat => calculatePrice(product, karat, buyer))) }; };
module.exports = { toProductResponse, slugify };
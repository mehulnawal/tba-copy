import {useEffect,useState} from "react";import {Link} from "react-router-dom";import {apiRequest} from "../api/client";import Navbar from "../components/Navbar";import Footer from "../components/Footer";type O={_id:string;orderStatus:"pending"|"confirmed"|"failed";amount:number;createdAt:string;items:{title:string;image:string;quantity:number;karat:string}[]};export default function OrderHistory(){const [orders,setOrders]=useState<O[]>([]);const [error,setError]=useState("");useEffect(()=>{apiRequest<O[]>("/orders").then(setOrders).catch(e=>setError(e.message))},[]);return <><Navbar onSearchChange={()=>{}} activeCategory="All" onCategoryChange={()=>{}}/><main className="mx-auto max-w-4xl p-8"><h1 className="text-3xl">My Orders</h1>{error&&<p>{error}</p>}{orders.map(o=><Link className="my-4 block border p-5" to={`/orderConfirmation?orderId=${o._id}`} key={o._id}><div><b>Order #{o._id}</b> · {o.orderStatus}</div><p>{new Date(o.createdAt).toLocaleDateString()}</p>{o.items.map((i,n)=><div key={n} className="mt-2 flex gap-3"><img src={i.image} className="h-14 w-14 object-cover"/><span>{i.title} · {i.karat.toUpperCase()} × {i.quantity}</span></div>)}<p>Total ₹{Math.round(o.amount).toLocaleString("en-IN")}</p></Link>)}{!orders.length&&!error&&<p>No orders yet.</p>}<p>For order tracking, contact us at {import.meta.env.VITE_ORDER_SUPPORT_PHONE||"our support team"}.</p></main><Footer onCategoryChange={()=>{}}/></>}